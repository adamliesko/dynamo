{application, dynamo, [
	{description, "DynamoDB inspired distributed key value store database"},
	{vsn, "0.1.0"},
	{modules, []},
	{registered, [dynamo_sup, storage_sup]},
	{applications, [
		kernel,
		stdlib,
		cowboy
	]},
	{mod, {dynamo_app, []}},
	{env, []}
]}.
